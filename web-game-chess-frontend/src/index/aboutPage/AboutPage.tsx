import classes from "./AboutPage.module.scss";

type AboutPageProps = {};

function AboutPage({}: AboutPageProps) {
  return <main className={classes["about-main"]}></main>;
}

export default AboutPage;
